import { marketTicks } from "../data/screens";
export default function TickerBar(){return <div className="flex h-10 items-center gap-6 overflow-x-auto whitespace-nowrap border-b border-outline-variant/20 bg-surface-container-low px-4 font-ticker text-sm">{marketTicks.map((tick)=><div key={tick.symbol} className="flex items-center gap-1.5"><span className="text-on-surface">{tick.symbol}</span><span className="font-bold text-on-surface">{tick.value}</span><span className={tick.neutral?"text-on-primary-container":tick.up?"text-teal":"text-red-soft"}>{tick.change}</span></div>)}</div>}
